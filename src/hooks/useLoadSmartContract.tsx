import { useMemo } from "react";
import { Axios } from "axios";

export default function useLoadSmartContract({
  contractAddress,
}: {
  contractAddress: string;
}) {
  const contractDataServer = useMemo(() => {});
}
